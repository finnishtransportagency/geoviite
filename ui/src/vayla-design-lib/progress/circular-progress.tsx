import * as React from 'react';
import styles from './circular-progress.scss';

const CircularProgress: React.FC = () => {
    return <div className={styles['circular-progress']} />;
};

export default CircularProgress;
